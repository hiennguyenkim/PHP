<?php
/**
 * @var Laminas\View\Renderer\PhpRenderer $this
 */
use Application\Core\Session;

$currentUser  = Session::get('user');
$isAdmin      = ($currentUser['role'] ?? '') === 'admin';
$currentRoute = $this->serverUrl(true);
?>
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <?= $this->headTitle('HCMUE Library')->setSeparator(' - ')->setAutoEscape(false) ?>
    <?= $this->headMeta() ?>
    <link rel="stylesheet" href="<?= $this->basePath('css/app.css') ?>">
</head>
<body>
<div class="page-shell">
    <header class="topbar">
        <div class="topbar-inner">
            <a class="brand" href="<?= $this->url('home') ?>">
                <span class="brand-mark">LIB</span>
                <span class="brand-copy">
                    <strong>HCMUE Library</strong>
                    <small>He thong quan ly thu vien Laminas MVC</small>
                </span>
            </a>

            <nav class="nav-links<?= $currentUser ? ' nav-links-authenticated' : '' ?>">
                <?php if ($currentUser): ?>
                    <a href="<?= $this->url('dashboard') ?>">Tong quan</a>
                    <?php if ($isAdmin): ?>
                        <a href="<?= $this->url('book') ?>">Sach</a>
                        <a href="<?= $this->url('loan') ?>">Muon tra</a>
                        <a href="<?= $this->url('user') ?>">Thanh vien</a>
                    <?php else: ?>
                        <a href="<?= $this->url('loan') ?>">Phieu muon cua toi</a>
                    <?php endif; ?>
                    <div class="user-chip">
                        <strong><?= htmlspecialchars((string)($currentUser['full_name'] ?? $currentUser['username'] ?? 'Nguoi dung'), ENT_QUOTES, 'UTF-8') ?></strong>
                        <small><?= htmlspecialchars(strtoupper((string)($currentUser['role'] ?? 'member')), ENT_QUOTES) ?></small>
                    </div>
                    <a href="<?= $this->url('auth', ['action' => 'logout']) ?>">Dang xuat</a>
                <?php else: ?>
                    <a href="<?= $this->url('auth', ['action' => 'login']) ?>">Dang nhap</a>
                    <a href="<?= $this->url('auth', ['action' => 'register']) ?>">Dang ky</a>
                <?php endif; ?>
            </nav>
        </div>
    </header>

    <main class="app-container">
        <?= $this->partial('application/partials/flash') ?>
        <?= $this->content ?>
    </main>

    <footer class="site-footer">
        <p>He thong quan ly thu vien theo chuan Laminas MVC &mdash; HCMUE &copy; <?= date('Y') ?></p>
    </footer>
</div>
<script src="<?= $this->basePath('js/app.js') ?>"></script>
</body>
</html>
