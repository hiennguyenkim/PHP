<?php
declare(strict_types=1);

$errors = $errors ?? [];
$old = $old ?? [];
?>
<section class="auth-layout">
    <article class="hero-card">
        <p class="eyebrow">Thanh vien thu vien</p>
        <h1>Tao tai khoan ban doc moi</h1>
        <p>
            Form dang ky se tao mot tai khoan thanh vien co the dang nhap vao he thong de theo doi ho so va lich su muon sach.
        </p>
        <ul class="feature-list">
            <li>Prepared statement chong SQL Injection.</li>
            <li>Password duoc bam bang <code>password_hash()</code>.</li>
            <li>Tai khoan moi mac dinh co vai tro <code>member</code> va trang thai <code>active</code>.</li>
        </ul>
    </article>

    <article class="panel auth-panel">
        <div class="section-head compact">
            <div>
                <p class="eyebrow">Authentication</p>
                <h2>Dang ky thanh vien</h2>
            </div>
            <a class="text-link" href="<?= $this->url('auth', ['action' => 'login']) ?>">Da co tai khoan</a>
        </div>

        <form method="post" action="<?= $this->url('auth', ['action' => 'register']) ?>" class="stack-form">
            <label class="field">
                <span>Ho va ten</span>
                <input
                    type="text"
                    name="full_name"
                    value="<?= htmlspecialchars((string) ($old['full_name'] ?? ''), ENT_QUOTES, 'UTF-8') ?>"
                    placeholder="Nguyen Van An"
                >
                <?php if (! empty($errors['full_name'])) : ?>
                    <small class="field-error"><?= htmlspecialchars((string) $errors['full_name'], ENT_QUOTES, 'UTF-8') ?></small>
                <?php endif; ?>
            </label>

            <label class="field">
                <span>Ten dang nhap</span>
                <input
                    type="text"
                    name="username"
                    value="<?= htmlspecialchars((string) ($old['username'] ?? ''), ENT_QUOTES, 'UTF-8') ?>"
                    placeholder="it nhat 3 ky tu"
                >
                <?php if (! empty($errors['username'])) : ?>
                    <small class="field-error"><?= htmlspecialchars((string) $errors['username'], ENT_QUOTES, 'UTF-8') ?></small>
                <?php endif; ?>
            </label>

            <label class="field">
                <span>Email</span>
                <input
                    type="email"
                    name="email"
                    value="<?= htmlspecialchars((string) ($old['email'] ?? ''), ENT_QUOTES, 'UTF-8') ?>"
                    placeholder="ban@example.com"
                >
                <?php if (! empty($errors['email'])) : ?>
                    <small class="field-error"><?= htmlspecialchars((string) $errors['email'], ENT_QUOTES, 'UTF-8') ?></small>
                <?php endif; ?>
            </label>

            <label class="field">
                <span>Mat khau</span>
                <input type="password" name="password" placeholder="toi thieu 6 ky tu">
                <?php if (! empty($errors['password'])) : ?>
                    <small class="field-error"><?= htmlspecialchars((string) $errors['password'], ENT_QUOTES, 'UTF-8') ?></small>
                <?php endif; ?>
            </label>

            <label class="field">
                <span>Nhap lai mat khau</span>
                <input type="password" name="confirm_password" placeholder="nhap lai mat khau">
                <?php if (! empty($errors['confirm_password'])) : ?>
                    <small class="field-error"><?= htmlspecialchars((string) $errors['confirm_password'], ENT_QUOTES, 'UTF-8') ?></small>
                <?php endif; ?>
            </label>

            <button type="submit" class="button button-primary full-width">Dang ky</button>
        </form>
    </article>
</section>
