<?php
declare(strict_types=1);

$users = $users ?? [];
$filters = $filters ?? [];
$indexUrl = $this->url('user');
$createUrl = $this->url('user', ['action' => 'create']);
?>
<section class="hero-surface">
    <div>
        <p class="eyebrow">Thanh vien thu vien</p>
        <h1>Quan ly thanh vien va tai khoan</h1>
        <p class="section-copy">Theo doi vai tro, trang thai va lich su muon sach cua tung thanh vien trong thu vien.</p>
    </div>
    <div class="hero-actions">
        <a class="button button-primary" href="<?= $createUrl ?>">Them thanh vien</a>
    </div>
</section>

<section class="panel">
    <form method="get" action="<?= $indexUrl ?>" class="filter-grid">
        <label class="field">
            <span>Tim kiem</span>
            <input
                type="text"
                name="search"
                value="<?= htmlspecialchars((string) ($filters['search'] ?? ''), ENT_QUOTES, 'UTF-8') ?>"
                placeholder="Ho ten, username hoac email"
            >
        </label>

        <label class="field">
            <span>Vai tro</span>
            <select name="role">
                <option value="">Tat ca vai tro</option>
                <option value="admin"<?= ($filters['role'] ?? '') === 'admin' ? ' selected' : '' ?>>Admin</option>
                <option value="member"<?= ($filters['role'] ?? '') === 'member' ? ' selected' : '' ?>>Member</option>
            </select>
        </label>

        <label class="field">
            <span>Trang thai</span>
            <select name="status">
                <option value="">Tat ca trang thai</option>
                <option value="active"<?= ($filters['status'] ?? '') === 'active' ? ' selected' : '' ?>>Hoat dong</option>
                <option value="inactive"<?= ($filters['status'] ?? '') === 'inactive' ? ' selected' : '' ?>>Tam khoa</option>
            </select>
        </label>

        <div class="filter-actions">
            <button type="submit" class="button button-primary">Loc du lieu</button>
            <a class="button button-secondary" href="<?= $indexUrl ?>">Dat lai</a>
        </div>
    </form>
</section>

<section class="panel">
    <div class="table-wrapper">
        <table class="data-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Thanh vien</th>
                    <th>Email</th>
                    <th>Vai tro</th>
                    <th>Trang thai</th>
                    <th>Dang muon</th>
                    <th>Thao tac</th>
                </tr>
            </thead>
            <tbody>
                <?php if ($users === []) : ?>
                    <tr>
                        <td colspan="7" class="table-empty">Chua co thanh vien nao phu hop bo loc hien tai.</td>
                    </tr>
                <?php else : ?>
                    <?php foreach ($users as $user) : ?>
                        <?php
                        $isCurrentUser = (int) (($currentUser['id'] ?? 0)) === (int) $user['id'];
                        $confirmMessage = 'Ban co chac chan muon xoa thanh vien ' . $user['full_name'] . ' khong?';
                        ?>
                        <tr>
                            <td>#<?= (int) $user['id'] ?></td>
                            <td>
                                <div class="cell-title">
                                    <?= htmlspecialchars((string) $user['full_name'], ENT_QUOTES, 'UTF-8') ?>
                                </div>
                                <small class="cell-subtitle">@<?= htmlspecialchars((string) $user['username'], ENT_QUOTES, 'UTF-8') ?></small>
                                <?php if ($isCurrentUser) : ?>
                                    <span class="badge">Tai khoan hien tai</span>
                                <?php endif; ?>
                            </td>
                            <td><?= htmlspecialchars((string) ($user['email'] ?? ''), ENT_QUOTES, 'UTF-8') ?></td>
                            <td><span class="badge"><?= htmlspecialchars(strtoupper((string) $user['role']), ENT_QUOTES, 'UTF-8') ?></span></td>
                            <td><span class="status-pill status-pill-<?= htmlspecialchars((string) $user['status'], ENT_QUOTES, 'UTF-8') ?>"><?= ($user['status'] ?? 'active') === 'active' ? 'Hoat dong' : 'Tam khoa' ?></span></td>
                            <td><?= (int) ($user['active_loans'] ?? 0) ?></td>
                            <td class="actions">
                                <a class="button button-secondary button-small" href="<?= $this->url('user', ['action' => 'edit', 'id' => (int) $user['id']]) ?>">Sua</a>
                                <a class="button button-secondary button-small" href="<?= $this->url('loan', [], ['query' => ['user_id' => (int) $user['id']]]) ?>">Phieu muon</a>
                                <form
                                    method="post"
                                    action="<?= $this->url('user', ['action' => 'delete', 'id' => (int) $user['id']]) ?>"
                                    class="inline-form"
                                    data-confirm="<?= htmlspecialchars($confirmMessage, ENT_QUOTES, 'UTF-8') ?>"
                                >
                                    <button type="submit" class="button button-danger button-small"<?= $isCurrentUser ? ' disabled' : '' ?>>Xoa</button>
                                </form>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                <?php endif; ?>
            </tbody>
        </table>
    </div>
</section>
