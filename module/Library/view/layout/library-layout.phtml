<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <?= $this->headTitle('HCMUE Library')->setSeparator(' | ')->setAutoEscape(false) ?>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        :root { --bs-primary: #0052a5; }
        .sidebar { background: #0052a5; min-height: 100vh; }
        .sidebar a { color: rgba(255,255,255,.8); text-decoration: none; }
        .sidebar a:hover, .sidebar a.active { color: #fff; background: rgba(255,255,255,.15); border-radius: .375rem; }
        .brand-logo { font-size: 1.3rem; font-weight: 700; color: #fff; }
        .stat-card { border-left: 4px solid var(--bs-primary); }
        .navbar-top { background: #fff; border-bottom: 1px solid #dee2e6; }
        .login-card { border-radius: 1rem; box-shadow: 0 20px 60px rgba(0,0,0,.18); }
        .guest-shell { background: linear-gradient(135deg, #0052a5 0%, #003d7a 100%); }
    </style>
</head>
<body class="bg-light">
<?php
$authUser = $this->currentUser();
$guestCatalogMode = ! $authUser && (bool) ($guestCatalogMode ?? false);
$bookRouteName = ($authUser['role'] ?? '') === 'admin' ? 'library/book' : 'catalog';
$shellClass = $authUser
    ? ''
    : ($guestCatalogMode ? ' min-vh-100' : ' min-vh-100 guest-shell');
$mainClass = $authUser
    ? 'p-4'
    : ($guestCatalogMode ? 'p-4 p-lg-5' : 'd-flex flex-grow-1 align-items-center justify-content-center p-4');
?>
<div class="d-flex<?= $shellClass ?>">
    <!-- Sidebar -->
    <?php if ($authUser) : ?>
    <nav class="sidebar p-3 d-flex flex-column" style="width:240px;flex-shrink:0">
        <div class="brand-logo mb-4">
            <i class="bi bi-book-half me-2"></i>HCMUE Library
        </div>
        <div class="text-white-50 small mb-4">Laminas MVC Library Management</div>
        <ul class="nav flex-column gap-1">
            <li class="nav-item">
                <a href="<?= $this->url('library/dashboard') ?>" class="nav-link px-3 py-2">
                    <i class="bi bi-speedometer2 me-2"></i>Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a href="<?= $this->url($bookRouteName) ?>" class="nav-link px-3 py-2">
                    <i class="bi bi-journals me-2"></i><?= $authUser['role'] === 'admin' ? 'Quản lý sách' : 'Danh mục sách' ?>
                </a>
            </li>
            <li class="nav-item">
                <a href="<?= $this->url('library/transaction') ?>" class="nav-link px-3 py-2">
                    <i class="bi bi-arrow-left-right me-2"></i><?= $authUser['role'] === 'admin' ? 'Mượn / Trả sách' : 'Phiếu mượn của tôi' ?>
                </a>
            </li>
            <?php if ($authUser['role'] === 'admin') : ?>
            <li class="nav-item">
                <a href="<?= $this->url('library/user') ?>" class="nav-link px-3 py-2">
                    <i class="bi bi-people me-2"></i>Người dùng
                </a>
            </li>
            <?php endif; ?>
        </ul>
        <div class="mt-auto">
            <hr class="border-white opacity-25">
            <div class="text-white-50 small mb-2">
                <i class="bi bi-person-circle me-1"></i><?= htmlspecialchars($authUser['full_name']) ?>
                <span class="badge bg-warning text-dark ms-1"><?= strtoupper($authUser['role'] !== '' ? $authUser['role'] : 'user') ?></span>
            </div>
            <a href="<?= $this->url('library/auth', ['action' => 'logout']) ?>" class="btn btn-sm btn-outline-light w-100">
                <i class="bi bi-box-arrow-left me-1"></i>Đăng xuất
            </a>
        </div>
    </nav>
    <?php endif; ?>

    <?php if ($guestCatalogMode) : ?>
        <div class="position-fixed top-0 end-0 p-3 p-md-4" style="z-index: 1030;">
            <a href="<?= $this->url('library/auth', ['action' => 'login']) ?>" class="btn btn-primary shadow-sm">
                <i class="bi bi-box-arrow-in-right me-1"></i>Đăng nhập
            </a>
        </div>
    <?php endif; ?>

    <!-- Main Content -->
    <div class="flex-grow-1 d-flex flex-column">
        <!-- Flash Messages -->
        <?php $flash = $this->flashMessenger(); ?>
        <?php if ($flash->hasSuccessMessages()) : ?>
            <div class="alert alert-success alert-dismissible m-3 mb-0" role="alert">
                <?php foreach ($flash->getSuccessMessages() as $msg) : ?>
                    <i class="bi bi-check-circle me-1"></i><?= htmlspecialchars($msg) ?>
                <?php endforeach; ?>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <?php endif; ?>
        <?php if ($flash->hasErrorMessages()) : ?>
            <div class="alert alert-danger alert-dismissible m-3 mb-0" role="alert">
                <?php foreach ($flash->getErrorMessages() as $msg) : ?>
                    <i class="bi bi-exclamation-triangle me-1"></i><?= htmlspecialchars($msg) ?>
                <?php endforeach; ?>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <?php endif; ?>
        <?php if ($flash->hasInfoMessages()) : ?>
            <div class="alert alert-info alert-dismissible m-3 mb-0" role="alert">
                <?php foreach ($flash->getInfoMessages() as $msg) : ?>
                    <?= htmlspecialchars($msg) ?>
                <?php endforeach; ?>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <?php endif; ?>

        <main class="<?= $mainClass ?>">
            <?= $this->content ?>
        </main>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
document.querySelectorAll('[data-confirm]').forEach(el => {
    el.addEventListener('submit', e => { if (!confirm(el.dataset.confirm)) e.preventDefault(); });
});
</script>
</body>
</html>
