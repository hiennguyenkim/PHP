<?php
$this->headTitle('Quản lý người dùng');
$this->layout('layout/library-layout');
?>
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4">
    <div>
        <h1 class="h3 fw-bold mb-0"><i class="bi bi-people me-2 text-primary"></i>Quản lý người dùng</h1>
        <p class="text-muted mb-0">Quản trị tài khoản cán bộ và sinh viên trong hệ thống thư viện.</p>
    </div>
    <a href="<?= $this->url('library/user', ['action' => 'add']) ?>" class="btn btn-primary">
        <i class="bi bi-person-plus me-1"></i>Thêm tài khoản
    </a>
</div>

<div class="row g-3 mb-4">
    <div class="col-md-4">
        <div class="card border-primary h-100">
            <div class="card-body">
                <div class="text-muted small mb-1">Tổng tài khoản</div>
                <div class="fs-3 fw-bold text-primary"><?= (int) ($summary['total'] ?? 0) ?></div>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card border-warning h-100">
            <div class="card-body">
                <div class="text-muted small mb-1">Quản trị viên</div>
                <div class="fs-3 fw-bold text-warning"><?= (int) ($summary['admins'] ?? 0) ?></div>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card border-info h-100">
            <div class="card-body">
                <div class="text-muted small mb-1">Sinh viên</div>
                <div class="fs-3 fw-bold text-info"><?= (int) ($summary['students'] ?? 0) ?></div>
            </div>
        </div>
    </div>
</div>

<div class="card shadow-sm border-0 mb-4">
    <div class="card-body">
        <form method="GET" class="row g-3 align-items-end">
            <div class="col-lg-7">
                <label class="form-label fw-semibold">Từ khóa</label>
                <input type="text" name="search" class="form-control"
                       value="<?= htmlspecialchars((string) ($filters['search'] ?? '')) ?>"
                       placeholder="Tìm theo họ tên hoặc tên đăng nhập">
            </div>
            <div class="col-lg-3">
                <label class="form-label fw-semibold">Vai trò</label>
                <select name="role" class="form-select">
                    <option value="">Tất cả vai trò</option>
                    <option value="admin" <?= ($filters['role'] ?? '') === 'admin' ? 'selected' : '' ?>>Quản trị viên</option>
                    <option value="student" <?= ($filters['role'] ?? '') === 'student' ? 'selected' : '' ?>>Sinh viên</option>
                </select>
            </div>
            <div class="col-lg-2 d-flex gap-2">
                <button type="submit" class="btn btn-primary w-100">
                    <i class="bi bi-search me-1"></i>Lọc
                </button>
                <a href="<?= $this->url('library/user') ?>" class="btn btn-light">Đặt lại</a>
            </div>
        </form>
    </div>
</div>

<div class="card shadow-sm border-0">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th style="width: 80px;">#</th>
                        <th>Họ và tên</th>
                        <th>Tên đăng nhập</th>
                        <th>Email</th>
                        <th>Vai trò</th>
                        <th>Ngày trả gần nhất</th>
                        <th style="width: 390px;">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                <?php if (! count($users)) : ?>
                    <tr>
                        <td colspan="7" class="text-center text-muted py-5">
                            Không tìm thấy tài khoản phù hợp.
                        </td>
                    </tr>
                <?php else : ?>
                    <?php foreach ($users as $user) : ?>
                        <tr>
                            <td class="text-muted"><?= (int) $user->id ?></td>
                            <td class="fw-semibold">
                                <?= htmlspecialchars($user->fullName) ?>
                                <?php if ((int) $user->id === (int) $currentId) : ?>
                                    <span class="badge bg-light text-dark border ms-1">Bạn</span>
                                <?php endif; ?>
                            </td>
                            <td><code>@<?= htmlspecialchars($user->username) ?></code></td>
                            <td><?= htmlspecialchars($user->email) ?></td>
                            <td>
                                <span class="badge <?= $user->role === 'admin' ? 'bg-warning text-dark' : 'bg-info text-dark' ?>">
                                    <?= $user->role === 'admin' ? 'Quản trị viên' : 'Sinh viên' ?>
                                </span>
                            </td>
                            <td><?= htmlspecialchars($user->lastReturnedAt !== '' ? $user->lastReturnedAt : '—') ?></td>
                            <td>
                                <div class="d-flex flex-wrap gap-2">
                                    <a href="<?= $this->url('library/user', ['action' => 'view', 'id' => $user->id]) ?>"
                                       class="btn btn-sm btn-outline-secondary">
                                        <i class="bi bi-eye me-1"></i>Chi tiết
                                    </a>
                                    <a href="<?= $this->url('library/user', ['action' => 'edit', 'id' => $user->id]) ?>"
                                       class="btn btn-sm btn-outline-primary">
                                        <i class="bi bi-pencil me-1"></i>Chỉnh sửa
                                    </a>
                                    <?php if ($user->role === 'student') : ?>
                                        <?php $confirmMessage = "Bạn có chắc muốn xóa sinh viên '" . $user->fullName . "'?"; ?>
                                        <form method="POST"
                                              action="<?= $this->url('library/user', ['action' => 'delete', 'id' => $user->id]) ?>"
                                              data-confirm="<?= htmlspecialchars($confirmMessage) ?>"
                                              class="d-inline">
                                            <button type="submit"
                                                    class="btn btn-sm btn-outline-danger"
                                                <?= (int) $user->id === (int) $currentId ? 'disabled' : '' ?>>
                                                <i class="bi bi-trash me-1"></i>Xóa
                                            </button>
                                        </form>
                                    <?php else : ?>
                                        <span class="text-muted small align-self-center">Chỉ xóa tài khoản sinh viên</span>
                                    <?php endif; ?>
                                </div>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
