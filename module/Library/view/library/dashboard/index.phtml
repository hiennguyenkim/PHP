<?php
$this->headTitle('Dashboard');
$this->layout('layout/library-layout');
$statusLabels = [
    'borrowed' => ['label' => 'Đang mượn', 'class' => 'warning'],
    'returned' => ['label' => 'Đã trả', 'class' => 'success'],
    'overdue' => ['label' => 'Quá hạn', 'class' => 'danger'],
];
?>
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4">
    <div>
        <h1 class="h3 fw-bold mb-0">Dashboard</h1>
        <p class="text-muted mb-0">
            <?= $isAdmin ? 'Bảng điều hành vận hành thư viện dành cho quản trị viên.' : 'Không gian theo dõi hoạt động mượn đọc của bạn.' ?>
        </p>
    </div>
    <div class="d-flex gap-2">
        <a href="<?= $this->url('library/book') ?>" class="btn btn-outline-primary">
            <i class="bi bi-journals me-1"></i><?= $isAdmin ? 'Mở kho sách' : 'Xem danh mục' ?>
        </a>
        <?php if ($isAdmin): ?>
            <a href="<?= $this->url('library/transaction', ['action' => 'borrow']) ?>" class="btn btn-primary">
                <i class="bi bi-plus-circle me-1"></i>Lập phiếu mượn
            </a>
        <?php endif; ?>
    </div>
</div>

<div class="card shadow-sm border-0 mb-4">
    <div class="card-body">
        <div class="d-flex flex-wrap justify-content-between align-items-center gap-3">
            <div>
                <div class="text-uppercase text-muted small fw-semibold">Xin chào</div>
                <div class="fs-4 fw-bold"><?= htmlspecialchars((string) ($currentUser['full_name'] ?? 'Người dùng thư viện')) ?></div>
            </div>
            <div class="text-muted">
                Vai trò hiện tại:
                <span class="badge <?= $isAdmin ? 'bg-warning text-dark' : 'bg-info text-dark' ?>">
                    <?= strtoupper(htmlspecialchars((string) ($currentUser['role'] ?? 'guest'))) ?>
                </span>
            </div>
        </div>
    </div>
</div>

<div class="row g-3 mb-4">
    <div class="col-md-3">
        <div class="card stat-card h-100 border-primary">
            <div class="card-body">
                <div class="text-muted small mb-1"><?= $isAdmin ? 'Tổng đầu sách' : 'Đầu sách trong thư viện' ?></div>
                <div class="fs-2 fw-bold text-primary"><?= (int) $totalBooks ?></div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card h-100 border-warning">
            <div class="card-body">
                <div class="text-muted small mb-1">Đang mượn</div>
                <div class="fs-2 fw-bold text-warning"><?= (int) $totalBorrowed ?></div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card h-100 border-danger">
            <div class="card-body">
                <div class="text-muted small mb-1">Quá hạn</div>
                <div class="fs-2 fw-bold text-danger"><?= (int) $totalOverdue ?></div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card h-100 border-success">
            <div class="card-body">
                <div class="text-muted small mb-1"><?= $isAdmin ? 'Thành viên sinh viên' : 'Sắp đến hạn trong 7 ngày' ?></div>
                <div class="fs-2 fw-bold text-success"><?= $isAdmin ? (int) $totalMembers : (int) $dueSoon ?></div>
            </div>
        </div>
    </div>
</div>

<div class="row g-4 mb-4">
    <div class="col-lg-5">
        <div class="card shadow-sm border-0 h-100">
            <div class="card-header bg-white border-0 pt-4 px-4">
                <h5 class="mb-0">Tình trạng kho sách</h5>
            </div>
            <div class="card-body px-4 pb-4">
                <div class="d-flex justify-content-between py-2 border-bottom">
                    <span class="text-muted">Đầu sách sẵn sàng</span>
                    <strong><?= (int) ($bookSummary['available_titles'] ?? 0) ?></strong>
                </div>
                <div class="d-flex justify-content-between py-2 border-bottom">
                    <span class="text-muted">Đầu sách đang mượn hết</span>
                    <strong><?= (int) ($bookSummary['borrowed_titles'] ?? 0) ?></strong>
                </div>
                <div class="d-flex justify-content-between py-2 border-bottom">
                    <span class="text-muted">Đầu sách tạm khóa</span>
                    <strong><?= (int) ($bookSummary['unavailable_titles'] ?? 0) ?></strong>
                </div>
                <div class="d-flex justify-content-between pt-2">
                    <span class="text-muted">Bản khả dụng hiện có</span>
                    <strong><?= (int) ($bookSummary['total_copies'] ?? 0) ?></strong>
                </div>
            </div>
        </div>
    </div>
    <div class="col-lg-7">
        <div class="card shadow-sm border-0 h-100">
            <div class="card-header bg-white border-0 pt-4 px-4">
                <h5 class="mb-0"><?= $isAdmin ? 'Hiệu suất lưu thông' : 'Tình trạng tài khoản mượn của bạn' ?></h5>
            </div>
            <div class="card-body px-4 pb-4">
                <div class="row g-3">
                    <div class="col-sm-6">
                        <div class="rounded-3 bg-light p-3 h-100">
                            <div class="text-muted small mb-1">Đã trả hoàn tất</div>
                            <div class="fs-3 fw-bold"><?= (int) $totalReturned ?></div>
                        </div>
                    </div>
                    <div class="col-sm-6">
                        <div class="rounded-3 bg-light p-3 h-100">
                            <div class="text-muted small mb-1"><?= $isAdmin ? 'Sắp đến hạn' : 'Khoản mượn sắp đến hạn' ?></div>
                            <div class="fs-3 fw-bold"><?= (int) $dueSoon ?></div>
                        </div>
                    </div>
                    <div class="col-12">
                        <div class="rounded-3 border p-3">
                            <div class="fw-semibold mb-2"><?= $isAdmin ? 'Khuyến nghị vận hành' : 'Khuyến nghị cá nhân' ?></div>
                            <div class="text-muted small">
                                <?php if ($isAdmin): ?>
                                    Ưu tiên xử lý các phiếu quá hạn và theo dõi những đầu sách đang cạn bản khả dụng để duy trì chất lượng phục vụ.
                                <?php else: ?>
                                    Hãy kiểm tra các khoản mượn sắp đến hạn để tránh phát sinh trạng thái quá hạn trong hồ sơ thư viện của bạn.
                                <?php endif; ?>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="card shadow-sm border-0">
    <div class="card-header bg-white d-flex justify-content-between align-items-center">
        <h5 class="mb-0"><i class="bi bi-clock-history me-2"></i><?= $isAdmin ? 'Phiếu mượn gần đây' : 'Hoạt động mượn gần đây của bạn' ?></h5>
        <a href="<?= $this->url('library/transaction') ?>" class="btn btn-sm btn-outline-primary">Xem tất cả</a>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th>#ID</th>
                        <th>Tên sách</th>
                        <?php if ($isAdmin): ?>
                            <th>Sinh viên</th>
                        <?php endif; ?>
                        <th>Ngày mượn</th>
                        <th>Hạn trả</th>
                        <th>Trạng thái</th>
                    </tr>
                </thead>
                <tbody>
                <?php if ($recentBorrows === []): ?>
                    <tr>
                        <td colspan="<?= $isAdmin ? '6' : '5' ?>" class="text-center text-muted py-4">
                            Chưa có dữ liệu mượn trả gần đây.
                        </td>
                    </tr>
                <?php else: ?>
                    <?php foreach ($recentBorrows as $record): ?>
                        <?php $status = $statusLabels[$record->status] ?? ['label' => $record->status, 'class' => 'secondary']; ?>
                        <tr>
                            <td class="text-muted">#<?= (int) $record->id ?></td>
                            <td class="fw-semibold"><?= htmlspecialchars($record->bookTitle) ?></td>
                            <?php if ($isAdmin): ?>
                                <td><?= htmlspecialchars($record->userFullName) ?></td>
                            <?php endif; ?>
                            <td><?= htmlspecialchars($record->borrowDate) ?></td>
                            <td><?= htmlspecialchars($record->returnDate) ?></td>
                            <td><span class="badge bg-<?= $status['class'] ?>"><?= $status['label'] ?></span></td>
                        </tr>
                    <?php endforeach; ?>
                <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
